import { PlacesSectionClient } from "@/components/places/PlacesSectionClient";

export default function AlternativePage() {
  return <PlacesSectionClient title="Alternative, okupa & street Berlin" categories={["alternative", "street_art", "park", "nightlife", "club"]} />;
}
